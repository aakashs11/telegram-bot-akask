steps:
  # Step 1: Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'asia-south1-docker.pkg.dev/telegram-bot-akask/telegram-bot-repo/telegram-bot:latest', '.']

  # Step 2: Push the Docker image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'asia-south1-docker.pkg.dev/telegram-bot-akask/telegram-bot-repo/telegram-bot:latest']

  # Step 3: Deploy the Docker image to Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'telegram-bot'
      - '--image'
      - 'asia-south1-docker.pkg.dev/telegram-bot-akask/telegram-bot-repo/telegram-bot:latest'
      - '--region'
      - 'asia-south1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--service-account=cloud-run-deployer@telegram-bot-akask.iam.gserviceaccount.com'
      - '--update-secrets'
      - 'OPENAI_API_KEY=projects/telegram-bot-akask/secrets/OPENAI_API_KEY:latest,TELEGRAM_BOT_TOKEN=projects/telegram-bot-akask/secrets/TELEGRAM_BOT_TOKEN:latest'
